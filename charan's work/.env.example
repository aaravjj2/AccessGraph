# Leave blank to use the canonical local mock. Set this ONE variable to the
# Orchestrator origin to use live mode, then restart Vite (or rebuild).
# VITE_ORCHESTRATOR_URL=http://127.0.0.1:8000
VITE_ORCHESTRATOR_URL=
